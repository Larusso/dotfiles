; clortho profile: cargo
; generated by:  clortho profile create
; edit with:     clortho profile edit cargo

; --- defines ---
(define clortho_env_cargo_cargo_home "${CLORTHO_ENV_CARGO_HOME:-${CLORTHO_ENV_HOME}/.cargo}")

; --- path rules ---
(allow file-read* file-write*
    (subpath clortho_env_cargo_cargo_home))
